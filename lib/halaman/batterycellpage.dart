import 'package:flutter/material.dart';

class MyBatteryCellPage extends StatelessWidget {
  const MyBatteryCellPage({super.key});

  @override
  Widget build(BuildContext context) {
   return const Center(
      child: Text('Informasi Baterai Per Sel'),
    );
  }
}